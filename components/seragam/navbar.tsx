import { Calendar, Home, Layers, LogOut } from 'lucide-react'
import type { UserSession } from './types'

export function Navbar({ currentView, onNavigate, user, onLogout }: { currentView: 'landing' | 'tracking'; onNavigate: (view: 'landing' | 'tracking') => void; user: UserSession; onLogout: () => void }) {
  const currentDate = new Date().toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' })
  return <header className="sticky top-0 z-40 border-b border-border bg-card/95 shadow-sm backdrop-blur no-print"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between gap-4 px-4 sm:px-6 lg:px-8"><button onClick={() => onNavigate('landing')} className="group flex min-w-0 items-center gap-3 text-left" id="navbar-brand-logo"><span className="flex size-11 shrink-0 items-center justify-center rounded-xl border border-border bg-background p-2 transition group-hover:border-primary/40"><img src="/yazaki-logo.jpeg" alt="Yazaki" className="max-h-full max-w-full object-contain" /></span><span className="min-w-0"><span className="flex items-center gap-2 truncate text-sm font-bold text-foreground">Data Seragam <span className="hidden rounded-full bg-primary/10 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wider text-primary sm:inline">PT JAI</span></span><span className="hidden truncate text-xs text-muted-foreground sm:block">PT Jatim Autocomp Indonesia</span></span></button><div className="flex items-center gap-2 sm:gap-4"><div className="hidden items-center gap-2 rounded-lg border border-border bg-muted/50 px-3 py-2 text-xs font-medium text-muted-foreground md:flex"><Calendar className="size-3.5 text-primary" />{currentDate}</div>{currentView === 'tracking' ? <button id="btn-nav-home" onClick={() => onNavigate('landing')} className="inline-flex items-center gap-2 rounded-lg bg-muted px-3.5 py-2 text-xs font-semibold text-foreground transition hover:bg-muted/70"><Home data-icon="inline-start" /><span className="hidden sm:inline">Menu Utama</span></button> : <button id="btn-nav-tracking" onClick={() => onNavigate('tracking')} className="inline-flex items-center gap-2 rounded-lg bg-primary px-3.5 py-2 text-xs font-semibold text-primary-foreground shadow-sm transition hover:-translate-y-0.5 hover:bg-primary/90"><Layers data-icon="inline-start" /><span>Data Seragam</span></button>}<div className="flex items-center gap-2 border-l border-border pl-2 sm:pl-4"><div className="hidden text-right sm:block"><div className="text-xs font-bold text-foreground">{user.fullName}</div><div className="text-[11px] text-muted-foreground">{user.role}</div></div><button id="btn-logout" onClick={onLogout} title="Keluar dari aplikasi" className="inline-flex items-center gap-1.5 rounded-lg px-2.5 py-2 text-xs font-semibold text-destructive transition hover:bg-destructive/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><LogOut data-icon="inline-start" /><span className="hidden sm:inline">Logout</span></button></div></div></div></header>
}
